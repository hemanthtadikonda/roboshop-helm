apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.component }}
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::043254050286:role/eks-admin
  labels:
    app.kubernetes.io/name: {{ .Values.component }}

